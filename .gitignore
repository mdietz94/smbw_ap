# switch-mod is a git submodule (fork at mdietz94/wondar). See .gitmodules.

# Claude Code state (per-developer).
.claude/

# Editor/IDE noise
.vscode/
.idea/
*.swp

# Python noise for manual_smbwonder_zim/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# vendor/Archipelago is a git submodule (pinned to commit 799e0b7b, matching
# the smo_archipelago sister project).  See .gitmodules.
venv/

# SMBW Client log (rotated per-launch, written by Utils.init_logging)
SMBWClient.txt

# Build output for the packaged .apworld zip (scripts/build_apworld.py)
build/

# OS noise
Thumbs.db
.DS_Store
