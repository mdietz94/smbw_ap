# switch-mod is a git submodule (fork at mdietz94/wondar). See .gitmodules.

# Claude Code state (per-developer).
.claude/

# Editor/IDE noise
.vscode/
.idea/
*.swp

# Python noise for manual_smbwonder_zim/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Vendored Archipelago checkout + its venv (per-developer).  Pinned to
# commit 799e0b7b -- see docs/m4-runbook.md for setup steps.
vendor/Archipelago/
venv/

# Bridge runtime log (rotated per-launch, per-developer)
bridge.log

# OS noise
Thumbs.db
.DS_Store
