# Next-session handoff prompt — M3.2 badge grant sprint

Copy-paste the block below into a new session. It is self-contained;
the new session will read the right docs from cold.

---

```
I'm continuing the M3.2 badge-grant static-analysis sprint for the
smwonder_archipelago project. Before doing anything else, please read
these in order (they have the full context):

1. CLAUDE.md (project orientation)
2. docs/handoff.md (current state — read first per CLAUDE.md)
3. docs/static-analysis-findings.md — the M3.2 sections, specifically
   from "2026-05-24 — M3.2 badge-grant sprint kickoff" onward (the
   sprint kicked off this session; 8 Ghidra rounds completed, sprint
   journal lives in this doc)

The TL;DR of where we are:

We're 8 Ghidra rounds into hunting the badge ownership writer. The
0xEA0 file-offset premise turned out to be wrong (it's a save-OUT
buffer offset). Multiple promising leads (FUN_7100348330, the q3 copy
pair, FUN_7101c62368) all turned out to be downstream consumers or
multiplayer session populators — NOT the badge ownership writer.

The latest finding is the most important: a previously-unknown
GameDataMgr accessor `FUN_7101F27B78(gmd*, void** out_obj_ptr,
uint32_t hash)` was discovered in FUN_7101c62368. It returns a
pointer to a GameDataField sub-object keyed by hash. Hypothesis: the
badge ownership bitfield (u64) is hash-keyed under SOME unknown hash
and accessible via this accessor.

There's a new Ghidra script at scripts/ghidra/walk_object_accessor_hashes.py
that walks every xref to FUN_7101F27B78, reconstructs the hash
constant at each callsite, and flags bit-iteration patterns (rbit /
clz / tbnz) nearby — those are u64 bitfield consumers.

Please:

1. Tell me to run scripts/ghidra/walk_object_accessor_hashes.py in
   Ghidra (I'll paste the output back).

2. From the output, identify the badge ownership hash candidates:
   - Hashes NOT already in the 8 known per-player session hashes
     (listed in static-analysis-findings.md run #8)
   - Hashes with the BIT-ITER NEARBY marker (rbit/clz/tbnz suggests
     u64 bitfield processing — bit iteration to enumerate set bits)
   - Hashes whose post-extract function is small enough to decompile
     and reveals u64-shaped reads (str/ldr x_ patterns)

3. For each strong candidate (probably 1-3), tell me which function
   to decompile next so we can confirm the read width and find the
   write path.

4. Once the badge ownership hash + access path is locked in, draft
   the M3.2 grant primitive in switch-mod/src/program/main.cpp
   following the pattern in static-analysis-findings.md run #8
   ("Updated grant primitive draft"). The grant should:
   - Take a badge internal_id (e.g., 9 = Coin Reward)
   - Resolve the live BadgeMgr pointer via the accessor
   - OR-equal (1ULL << internal_id) into the bitfield

5. Then suggest a verification protocol via save-diff:
   GrantBadge(9) → trigger save → diff game_data.sav → bit 9 of u64
   at file offset 0x0EA0 should be set.

Important notes (don't repeat past mistakes):

- Sprint 1's 7 string-grep badge scripts (find_badge_*.py,
  inspect_badge_*.py) dead-ended at test harness FUN_7101b1fb6c.
  Don't repeat — use dataflow-anchored search.

- The "0xEA0 file offset = live offset" assumption was WRONG. Live
  badge state is in a separate struct accessed via the new accessor
  pattern, not at file-offset coordinates.

- Cheat Engine memory-write breakpoints didn't fire on candidate
  addresses — user confirms; continue with static analysis.

- FUN_7100348330 is NOT the badge ownership query (it's a per-stage
  attribute check). The Magnet Coin / Wall-Jump cheat anchors are
  downstream of ownership.

- badge_id_array is an INLINE array of per-player equipped badge IDs
  (P1/P2/P3 slots populated via hashes 0x6ba8ad3d / 0x1415f836 /
  0xb1ae38a4), NOT a sead::Buffer<int> and NOT the owned bitfield.

After tackling steps 1-5, please append a "Run #9" section to
docs/static-analysis-findings.md following the existing append-only
style.
```

---

## Quick reference for the new session

After the handoff is processed, this is the minimal context the new
session needs to be productive:

- **Working dir**: `C:\Users\maxwe\Documents\smwonder_archipelago\.claude\worktrees\pensive-nobel-c5c3b7\`
- **Script to run**: `scripts/ghidra/walk_object_accessor_hashes.py`
- **Target function**: `FUN_7101F27B78` (NSO +0x01F27B78)
- **Doc to append to**: `docs/static-analysis-findings.md`
- **Code to update if successful**: `switch-mod/src/program/main.cpp`

## Known hash key corpus (skip these as candidates)

From CLAUDE.md and run #8 — these are already mapped, so ignore them
in the new script's output:

| Hash | Field |
|---|---|
| `0xf4ee6827` | flower_coin |
| `0x17f0bb21` | regular_coin |
| `0x55815859` | GRAND_SEED_WORLD1 |
| `0x49abba86` | GRAND_SEED_WORLD2 |
| `0xb550d8d6` | GRAND_SEED_WORLD3 |
| `0x1dcf7f6e` | GRAND_SEED_WORLD4 |
| `0x0d5a3e00` | GRAND_SEED_WORLD5 |
| `0xd4660d2b` | GRAND_SEED_WORLD6 |
| `0x5d3ec9b4` | COMPLETE_GAME |
| `0x89f1cc52` | INTRO_CUTSCENE_COMPLETED |
| `0xdf82e9ab` | course-clear lookup |
| `0xed817774` | touch_goal_top_result-related bool |
| `0xf79bcbb0` | current course goal_id counter |
| `0x2d8c6ec0`-`0xf0d05e3a` (5 keys) | per-player session fields |
| `0x6ba8ad3d` | badge_id_array[0] (P1 equipped) |
| `0x1415f836` | badge_id_array[1] (P2 equipped) |
| `0xb1ae38a4` | badge_id_array[2] (P3 equipped) |
| `0x3b241921`, `0x1b56d494`, `0x5bf37fa9`, `0xb8c53575`, `0xf20e6a36` | course-event reporter |
