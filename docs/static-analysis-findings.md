# Moved → bundled with the `smbw-reverse-engineering` skill

This file (the master GameDataMgr / RE decompile journal) now lives at
[`.claude/skills/smbw-reverse-engineering/reference/static-analysis-findings.md`](../.claude/skills/smbw-reverse-engineering/reference/static-analysis-findings.md).

It travels with the **smbw-reverse-engineering** skill — invoke that skill
(or open the reference directly) for the full RE findings. This stub only
exists so older `docs/...` links keep resolving.
