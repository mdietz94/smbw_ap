# Moved → bundled with the `smbw-logic` skill

The logic reconciliation record (the logic-PDF diff resolution, the badge
progression-wall rule, region-gate facts, and known limitations) now lives at
[`.claude/skills/smbw-logic/reference/logic-reconciliation.md`](.claude/skills/smbw-logic/reference/logic-reconciliation.md).

It travels with the **smbw-logic** skill — reach for that skill when editing the
apworld logic (region gates, `requires` strings, badge/seed gating) or debugging
a generation/softlock issue. This stub only exists so older `LOGIC_COMPARISON.md`
links keep resolving.
